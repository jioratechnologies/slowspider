{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "NestJS port of /api/v1/** — parallel backend, Phase 1 of docs/MIGRATION-PLAN-bff-kong-split.md",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:status": "prisma migrate status",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.6",
    "@prisma/client": "^7.9.1",
    "@supabase/supabase-js": "^2.112.3",
    "dotenv": "^17.4.2",
    "ioredis": "^6.0.0",
    "reflect-metadata": "^0.2.2",
    "resend": "^6.19.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "@types/express": "^5.0.0",
    "@types/node": "^20",
    "prisma": "^7.9.1",
    "typescript": "^5"
  }
}

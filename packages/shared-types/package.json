{
  "name": "@slowspider/shared-types",
  "version": "0.0.1",
  "private": true,
  "description": "DTO/API contract types shared by apps/web, apps/backend and apps/mobile — see docs/MIGRATION-PLAN-bff-kong-split.md (Phase 0: extract packages/shared-types; wired up for real in the follow-up noted there).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
